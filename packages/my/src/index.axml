<canvas
  a:if="{{id}}"
  type="{{type}}"
  id="{{id}}"
  class="f2-canvas"
  width="{{width}}"
  height="{{height}}"
  onTouchStart="touchStart"
  onTouchMove="touchMove"
  onTouchEnd="touchEnd"
  onReady="onCanvasReady"
/>
